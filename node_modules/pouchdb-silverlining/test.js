var PouchDB = require('pouchdb');
PouchDB.plugin(require('pouchdb-find'));
var silverlining = require('.');
PouchDB.plugin(silverlining);
var db = new PouchDB('animals');
/*db.bulkDocs([
  {
    _id: 'mittens',
    occupation: 'kitten',
    cuteness: 9.0
  },
  {
    _id: 'katie',
    occupation: 'kitten',
    cuteness: 7.0
  },
  {
    _id: 'felix',
    occupation: 'kitten',
    cuteness: 8.0
  }
]).then(console.log);*/
db.sql("SELECT * FROM cats").then(console.log);

//db.sql("SELECT * FROM cats WHERE occupation='kitten' AND cuteness >= 8.0").then(console.log);
//db.sql("SELECT * from cats WHERE _id='felix'").then(console.log).catch(console.error);